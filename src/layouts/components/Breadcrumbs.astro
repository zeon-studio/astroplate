---
import { humanize } from "@/lib/utils/textConverter";

const { className }: { className?: string } = Astro.props;

// 1. 获取并过滤掉空字符串
const paths = Astro.url.pathname.split("/").filter((x) => x);
let parts = [
  {
    label: "首页", // 始终显示中文
    href: "/",
    "aria-label": Astro.url.pathname === "/" ? "页" : undefined,
  },
];

paths.forEach((segment: string, i: number) => {
  // 跳过 "page" 路径，因为它只是分页标识，我们在后序路径中处理
  if (segment === "page") return;
  
  const href = `/${paths.slice(0, i + 1).join("/")}`;
  
  // 🌟 核心修复 1：在处理之前，对 URL 编码的段进行解码
  const decodedSegment = decodeURIComponent(segment);

  let displayLabel = decodedSegment.replace(".html", "").replace(/[-_]/g, " ");
  
  // 🌟 核心修复 2：处理特定的英文路径，进行中文化
  if (displayLabel.toLowerCase() === "tags") {
    displayLabel = "标签";
  } else if (displayLabel.toLowerCase() === "categories") {
    displayLabel = "分类";
  } else {
    // 对解码后的段运行 humanize 进行美化 (如 'ren-xiang' -> 'Ren Xiang')
    displayLabel = humanize(displayLabel);
  }
  
  // 避免添加空标签或重复标签
  if (displayLabel) {
    parts.push({
      label: displayLabel,
      href,
      "aria-label": Astro.url.pathname === href ? "页" : undefined,
    });
  }
});
---

<nav aria-label="Breadcrumb" class={className}>
  <ol class="inline-flex" role="list">
    {
      parts.map(({ label, ...attrs }, index) => (
        <li class="mx-1 capitalize" role="listitem">
          {index > 0 && <span class="inlin-block mr-1">/</span>}
          {index !== parts.length - 1 ? (
            <a class="text-primary dark:text-darkmode-primary" {...attrs}>
              {label}
            </a>
          ) : (
            <span class="text-text-light dark:text-darkmode-text-light">
              {label}
            </span>
          )}
        </li>
      ))
    }
  </ol>
</nav>
