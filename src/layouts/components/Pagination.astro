---
type Pagination = {
  section?: string;
  currentPage?: number;
  totalPages?: number;
};
const { section, currentPage = 1, totalPages = 1 }: Pagination = Astro.props;

const indexPageLink = currentPage === 2;
const hasPrevPage = currentPage > 1;
const hasNextPage = totalPages > currentPage!;

let pageList: number[] = [];
for (let i = 1; i <= totalPages; i++) {
  pageList.push(i);
}
---

{
  totalPages > 1 && (
    <nav
      aria-label="Pagination"       class="flex items-center justify-center space-x-3"
    >
      {/* previous */}
      {hasPrevPage ? (
        <a
          class="rounded px-2 py-1.5 text-text-dark hover:bg-light dark:text-darkmode-text-dark dark:hover:bg-darkmode-light"           href={
            indexPageLink
              ? `${section ? "/" + section : "/"}`
              : `${section ? "/" + section : ""}/page/${currentPage - 1}`
          }
        >
          <span class="sr-only">Previous</span>
          <svg
            aria-hidden="true"             fill="currentColor"             height="30"             viewBox="0 0 20 20"
            width="30"
          >
            <path
              clip-rule="evenodd"               d="M12.707 5.293a1 1 0 010 1.414L9.414 10l3.293 3.293a1 1 0 01-1.414 1.414l-4-4a1 1 0 010-1.414l4-4a1 1 0 011.414 0z"               fill-rule="evenodd"
            />
          </svg>
        </a>
      ) : (
        <span class="rounded px-2 py-1.5 text-text-light">
          <span class="sr-only">Previous</span>
          <svg
            aria-hidden="true"             fill="currentColor"             height="30"             viewBox="0 0 20 20"
            width="30"
          >
            <path
              clip-rule="evenodd"               d="M12.707 5.293a1 1 0 010 1.414L9.414 10l3.293 3.293a1 1 0 01-1.414 1.414l-4-4a1 1 0 010-1.414l4-4a1 1 0 011.414 0z"               fill-rule="evenodd"
            />
          </svg>
        </span>
      )}

      {/* page index */}
      {pageList.map((pagination, i) =>
        pagination === currentPage ? (
          <span
            aria-current="page"
            class="rounded bg-primary px-4 py-2 text-white dark:bg-darkmode-primary dark:text-text-dark"
          >
            {pagination}
          </span>
        ) : (
          <a
            aria-current="page"             class="rounded px-4 py-2 text-text-dark hover:bg-light dark:text-darkmode-text-dark dark:hover:bg-darkmode-light"             href={
              i === 0
                ? `${section ? "/" + section : "/"}`
                : `${section ? "/" + section : ""}/page/${pagination}`
            }
          >
            {pagination}
          </a>
        )
      )}

      {/* next page */}
      {hasNextPage ? (
        <a
          class="rounded px-2 py-1.5 text-text-dark hover:bg-light dark:text-darkmode-text-dark dark:hover:bg-darkmode-light"           href={`${section ? "/" + section : ""}/page/${currentPage + 1}`}
        >
          <span class="sr-only">Next</span>
          <svg
            aria-hidden="true"             fill="currentColor"             height="30"             viewBox="0 0 20 20"
            width="30"
          >
            <path
              clip-rule="evenodd"               d="M7.293 14.707a1 1 0 010-1.414L10.586 10 7.293 6.707a1 1 0 011.414-1.414l4 4a1 1 0 010 1.414l-4 4a1 1 0 01-1.414 0z"               fill-rule="evenodd"
            />
          </svg>
        </a>
      ) : (
        <span class="rounded px-2 py-1.5 text-text-light">
          <span class="sr-only">Next</span>
          <svg
            aria-hidden="true"             fill="currentColor"             height="30"             viewBox="0 0 20 20"
            width="30"
          >
            <path
              clip-rule="evenodd"               d="M7.293 14.707a1 1 0 010-1.414L10.586 10 7.293 6.707a1 1 0 011.414-1.414l4 4a1 1 0 010 1.414l-4 4a1 1 0 01-1.414 0z"               fill-rule="evenodd"
            />
          </svg>
        </span>
      )}
    </nav>
  )
}
