---
import config from "@/config/config.json";
import ImageMod from "./ImageMod.astro"; 

const { src, srcDarkmode }: { src?: string; srcDarkmode?: string } =
  Astro.props;
const {
  logo,
  logo_darkmode,
  logo_width,
  logo_height,
  logo_text,
  title,
}: {
  logo: string;
  logo_darkmode: string;
  logo_width: any;
  logo_height: any;
  logo_text: string;
  title: string;
} = config.site;

const { theme_switcher }: { theme_switcher: boolean } = config.settings;

// 1. 获取主 logo 路径
let primaryLogoPath = src || logo;
let primaryLogoSrc = primaryLogoPath && String(primaryLogoPath).trim() !== ''
                     ? (!primaryLogoPath.startsWith('http') 
                        // 修复后的路径逻辑 (删除 /public)
                        ? (primaryLogoPath.startsWith('/') ? primaryLogoPath : `/${primaryLogoPath}`)
                        : primaryLogoPath)
                     : null; 

// 2. 获取深色模式 logo 路径
let darkmodeLogoPath = srcDarkmode || logo_darkmode;
let darkmodeLogoSrc = darkmodeLogoPath && String(darkmodeLogoPath).trim() !== ''
                      ? (!darkmodeLogoPath.startsWith('http')
                         // 修复后的路径逻辑 (删除 /public)
                         ? (darkmodeLogoPath.startsWith('/') ? darkmodeLogoPath : `/${darkmodeLogoPath}`)
                         : darkmodeLogoPath)
                      : null; 

// 3. 安全解析 logo 尺寸
const logoW = parseInt(String(logo_width).replace("px", "")) || 100;
const logoH = parseInt(String(logo_height).replace("px", "")) || 30;
---

<a href="/" class="navbar-brand inline-block">
  {
    // 只有 primaryLogoSrc 或 darkmodeLogoSrc 存在且非空时，才渲染图片元素
    (primaryLogoSrc || darkmodeLogoSrc) ? (
      <>
        {/* 常规模式 Logo (仅在 primaryLogoSrc 有效时渲染) */}
        {primaryLogoSrc && (
          <ImageMod
            src={primaryLogoSrc} 
            class={`inline-block ${theme_switcher && "dark:hidden"}`}
            width={logoW * 2}
            height={logoH * 2}
            alt={title}
            style={{
              height: logoH + "px",
              width: logoW + "px",
            }}
            format="webp"
          />
        )}
        
        {/* 深色模式 Logo (仅在 theme_switcher 开启且 darkmodeLogoSrc 有效时渲染) */}
        {theme_switcher && darkmodeLogoSrc && (
          <ImageMod
            src={darkmodeLogoSrc} 
            class={"hidden dark:inline-block"}
            width={logoW * 2}
            height={logoH * 2}
            alt={title}
            style={{
              height: logoH + "px",
              width: logoW + "px",
            }}
            format="webp"
          />
        )}
      </>
    ) : logo_text ? (
      logo_text
    ) : (
      title
    )
  }
</a>