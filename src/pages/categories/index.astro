---
import Base from "@/layouts/Base.astro";
import { getAllTaxonomy, getTaxonomy } from "@/lib/taxonomyParser.js";
// 🌟 关键修正：导入 slugifyTitle 以确保链接的兼容性
import { humanize, slugifyTitle } from "@/lib/utils/textConverter"; 
import PageHeader from "@/partials/PageHeader.astro";

const BLOG_FOLDER = "blog";

// 保持：这里定义为 category (单数)
const category = await getTaxonomy(BLOG_FOLDER, "category");
const allCategory = await getAllTaxonomy(BLOG_FOLDER, "category");
---

<Base title="分类">
  <PageHeader title="分类" />
  <section class="section">
    <div class="container text-center">
      <ul>
        {
          // 🚀 核心修复：使用 category (单数) 变量进行迭代
          category.map((cat: string) => { 
            const count = allCategory.filter((c) => c === cat).length;
            return (
              <li class="m-3 inline-block">
                <a
                  // 使用 slugifyTitle 生成 URL
                  href={`/categories/${slugifyTitle(cat)}`}
                  class="block rounded bg-light px-4 py-2 text-xl text-text-dark dark:bg-darkmode-light dark:text-darkmode-text-dark"
                >
                  {humanize(cat)}{" "}
                  <span class="ml-2 rounded bg-body px-2 dark:bg-darkmode-body">
                    {count}
                  </span>
                </a>
              </li>
            );
          })
        }
      </ul>
    </div>
  </section>
</Base>
