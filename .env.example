# EmDash CMS secrets — copy this file to .env and fill in your own values
# Generate a secret: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"

EMDASH_AUTH_SECRET=replace_with_a_random_secret_at_least_32_chars
EMDASH_PREVIEW_SECRET=replace_with_a_random_preview_secret
